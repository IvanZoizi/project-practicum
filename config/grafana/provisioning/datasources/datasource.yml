apiVersion: 1

datasources:
  - name: ds_prometeus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    uid: ds_prometeus
    jsonData:
      timeInterval: "5s"
      queryTimeout: "30s"
      httpMethod: "POST"